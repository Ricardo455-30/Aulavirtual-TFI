// API Base URL
const API_BASE = "http://localhost:8000/api";

// Endpoints
export const CURSOS_URL = `${API_BASE}/cursos`;
export const ALUMNOS_URL = `${API_BASE}/alumnos`;
export const ASISTENCIAS_URL = `${API_BASE}/asistencias`;
export const CALIFICACIONES_URL = `${API_BASE}/calificaciones`;
export const TAREAS_URL = `${API_BASE}/tareas`;
export const MATERIAS_URL = `${API_BASE}/materias`;
export const AUTH_URL = `${API_BASE}/auth`;
export const USUARIOS_URL = `${API_BASE}/users`;